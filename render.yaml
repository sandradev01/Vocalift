services:
  - type: web
    name: vocalift
    env: docker
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: "1"
      - key: MAX_CONTENT_LENGTH
        value: "52428800"
